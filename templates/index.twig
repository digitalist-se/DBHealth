{% extends 'admin.twig' %}
{% block content %}
<div class="card">
    <div class="card-content">
        <div class="row">
            {{ dataTable | raw }}
        </div>
    </div>
</div>

{% endblock %}
